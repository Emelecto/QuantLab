// Todos los cursos están abiertos: el QP se sigue ganando con quiz
// perfecto pero nada se bloquea. Se conserva el componente por compat.
export function AcademiaCourseGate() {
  return <span className="academia-kicker">Acceso libre</span>;
}
